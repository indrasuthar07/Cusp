[project]
name = "cusp-workspace"
version = "0.0.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["services/*", "packages/*"]

[tool.uv]
dev-dependencies = ["ruff", "mypy", "pytest", "pytest-asyncio"]