[project]
name = "cusp-sdk-python"
version = "0.1.0"
description = "Python SDK for Cusp Gateway"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
